import React from 'react';
import Button from '../component/Button/Button';

class Home extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div>
            <Button value="确定"/>
            </div>
        );
    }
}

export default Home;
